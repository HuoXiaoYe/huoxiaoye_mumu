import React, {useState,useEffect} from "react"

type type = 'first' | 'second'

interface IProps {
    type : type
}

function Title(props:Iprops){
    const calssname = useState<string>(123);

    return (
        <div className="mu-title-wrap">

        </div>
    )
}

export default Title